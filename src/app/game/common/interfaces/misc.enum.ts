export enum EGender {
	Male   = 1,
	Female = 2
}

export enum LogMessageType {
	GENERAL = 1,
	EXPLORE = 2,
	COMBAT  = 3
}

export enum ESpawnType {
	NPC    = 'npc',
	OBJECT = 'object',
	ITEM   = 'item'
}